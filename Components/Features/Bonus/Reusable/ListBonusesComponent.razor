@if (showId)
{
    @foreach (var bonus in bonuses)
    {
        <p>@bonus.BonusId: @bonus.ToString()</p>
    }
}
else
{
    @foreach (var bonus in bonuses)
    {
        <p>@bonus.ToString()</p>
    }
}

@code {
    [Parameter]
    public required List<Bonus> bonuses { get; set; }
    [Parameter]
    public required bool showId { get; set; }
}